version: 1
frontend:
  phases:
    build:
      commands: 
        - echo "No build required for static site"
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
